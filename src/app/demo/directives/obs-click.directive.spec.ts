import { ObsClickDirective } from './obs-click.directive';

describe('ObsClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ObsClickDirective();
    expect(directive).toBeTruthy();
  });
});
